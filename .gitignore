node_modules/
*.tsbuildinfo

# Local configuration and credentials never enter this repository.
.env
.env.*
*.key
*.pem
auth.json
credentials.json
cycle-local/

# Scratch and debug output.
tmp/
.tmp/
coverage/
*.log

# Packaged artifacts.
build/

# Editor and OS noise.
.DS_Store
Thumbs.db
.idea/
.vscode/

# Internal only: the readiness plan, the certification matrix and its recorded results, and the
# benches that drive the manual rows. They stay on the maintainer machines rather than in the
# published repository, which carries the plugin and its own test suite.
documentation/
tests-debug/
