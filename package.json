{
  "name": "cycle-control-plane",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Control plane for the Cycle plugin. No runtime dependencies.",
  "license": "FSL-1.1-MIT",
  "author": "Gianluca Iannotta",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "check": "npm run typecheck && npm run build && npm test && npm run package",
    "package": "node scripts/package.mjs",
    "test": "node --test \"tests/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.tests.json --pretty false"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  }
}
