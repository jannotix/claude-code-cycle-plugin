{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "cycle",
  "displayName": "Cycle",
  "version": "1.1.1",
  "description": "Evidence-gated delivery: an architect, an executor, two independent reviewers and an independent arbiter that judges against your original request.",
  "author": {
    "name": "Gianluca Iannotta",
    "url": "https://github.com/jannotix"
  },
  "homepage": "https://github.com/jannotix/claude-code-cycle-plugin",
  "repository": "https://github.com/jannotix/claude-code-cycle-plugin",
  "license": "FSL-1.1-MIT",
  "keywords": [
    "code-review",
    "verification",
    "quality-gates",
    "multi-agent",
    "delivery"
  ],
  "userConfig": {
    "architect_model": {
      "type": "string",
      "title": "Architect model",
      "description": "Model for planning and task decomposition. Use inherit to follow the session model.",
      "default": "inherit"
    },
    "executor_model": {
      "type": "string",
      "title": "Executor model",
      "description": "Model for implementation. This role performs the most tool calls, so prefer a model with reliable tool use.",
      "default": "inherit"
    },
    "functional_reviewer_model": {
      "type": "string",
      "title": "Functional reviewer model",
      "description": "Model for the completeness and behaviour review. Independence improves when this differs from the security reviewer and the arbiter.",
      "default": "inherit"
    },
    "security_reviewer_model": {
      "type": "string",
      "title": "Security reviewer model",
      "description": "Model for the security and architecture review.",
      "default": "inherit"
    },
    "arbiter_model": {
      "type": "string",
      "title": "Arbiter model",
      "description": "Model for the final verdict. Correlating this with the architect weakens the boundary it exists to enforce.",
      "default": "inherit"
    },
    "council_models": {
      "type": "string",
      "title": "Council models",
      "description": "Comma-separated models for the advisory council, which answers a question independently before one seat synthesises. Leave empty to run every seat on the architect's model: the answers stay independent, the blind spots do not, and agreement between them is not corroboration.",
      "default": ""
    },
    "council_chairman_model": {
      "type": "string",
      "title": "Council chairman model",
      "description": "Model that synthesises the council's answers. Leave empty to use the architect's.",
      "default": ""
    },
    "council_size": {
      "type": "string",
      "title": "Council size",
      "description": "Seats to run when council_models names none: 2 to 5. Below two there is nothing to disagree about.",
      "default": "3"
    },
    "operator_model": {
      "type": "string",
      "title": "Operator model",
      "description": "Model for deterministic control-plane calls. A small fast model is sufficient.",
      "default": "haiku"
    },
    "architect_effort": {
      "type": "string",
      "title": "Architect effort",
      "description": "low, medium, high, xhigh or max.",
      "default": "high"
    },
    "executor_effort": {
      "type": "string",
      "title": "Executor effort",
      "description": "low, medium, high, xhigh or max.",
      "default": "high"
    },
    "reviewer_effort": {
      "type": "string",
      "title": "Reviewer effort",
      "description": "Applies to both independent reviewers. low, medium, high, xhigh or max.",
      "default": "high"
    },
    "arbiter_effort": {
      "type": "string",
      "title": "Arbiter effort",
      "description": "low, medium, high, xhigh or max.",
      "default": "high"
    },
    "max_repair_cycles": {
      "type": "number",
      "title": "Repair cycles",
      "description": "Rejected candidates repaired before the workflow blocks. The blocked state preserves all work and can be resumed.",
      "default": 5,
      "min": 1,
      "max": 20
    },
    "security_proofs": {
      "type": "string",
      "title": "Execute security proofs",
      "description": "off keeps proofs disabled: the security reviewer states a vulnerability in the review instead of running code to show it, and an undemonstrated critical is downgraded rather than discarded. on lets the reviewer execute a proof it writes against a throwaway copy of the candidate, with this account's privileges and no operating-system sandbox.",
      "default": "off"
    },
    "gate_strictness": {
      "type": "string",
      "title": "Gate strictness",
      "description": "standard fails a change whose layer has no proof. strict also fails on skipped gates. advisory downgrades missing gates to warnings.",
      "default": "standard"
    },
    "data_dir": {
      "type": "directory",
      "title": "Data directory",
      "description": "Where workflow state, history, memory and the code index are stored. Leave empty for a directory outside anything the application manages, so a history of delivered work outlives the plugin and survives uninstalling it."
    }
  }
}
